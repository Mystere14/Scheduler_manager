services:
  db:
    image: postgres:15
    container_name: data_db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: database
    ports:
      - "5433:5432"
